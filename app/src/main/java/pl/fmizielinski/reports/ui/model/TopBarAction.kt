package pl.fmizielinski.reports.ui.model

import androidx.annotation.StringRes
import pl.fmizielinski.reports.R

enum class TopBarAction(@StringRes val nameResId: Int) {
    REGISTER(R.string.common_button_register),
}
